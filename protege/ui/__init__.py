"""Tkinter user interface.

Deliberately plain: system default widgets, no theming, no custom styling, no
icons, no splash screen, no animation. A utility, not a product.

The one place visual emphasis is spent is on blocked responses and on warnings
that a lock layer is disabled, because those are the states a user must not
skim past.
"""
