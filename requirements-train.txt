# The training environment (E4): models/train/env. NOT Akira's own Python.
#
# These train LoRA adapters on this computer's graphics card. They include
# Hugging Face's libraries, which carry a model downloader (huggingface_hub,
# httpx, hf-xet), so they are kept out of Akira's own environment altogether,
# and requirements.txt's "Deliberately NOT used" still holds for it. The
# trainer, akira/training/_trainer.py, runs here as a process of its own: it
# installs Akira's network guard and forces every library offline before it
# imports any of them, and loads the base model only from its folder
# (local_files_only). verify_offline.py holds both rules. The person approved
# this exception on 2026-09-25.
#
# Set up once:
#   python -m venv models/train/env
#   models/train/env/Scripts/python -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cu130
#   models/train/env/Scripts/python -m pip install -r requirements-train.txt
#
# Pinned exactly, as installed.

accelerate==1.15.0
annotated-doc==0.0.5
anyio==4.15.1
bitsandbytes==0.50.2
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
filelock==3.32.3
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.33.0
idna==3.20
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.3
packaging==26.3
peft==0.21.0
psutil==7.2.2
Pygments==2.21.0
PyYAML==6.0.3
regex==2026.9.10
rich==15.0.0
safetensors==0.8.0
setuptools==78.1.0
shellingham==1.5.4
sympy==1.14.0
tokenizers==0.23.2
# torch==2.14.0+cu130 comes from the PyTorch index, above.
tqdm==4.70.1
transformers==5.17.0
typer==0.27.2
typing_extensions==4.16.0
