"""Domain logic: configuration, models, conversations.

Nothing here imports Qt. The interface layer sits above it and may be replaced
without touching any of this — which is exactly what happened to the Tkinter
one.
"""
